---
scope: Howard Hertzog WWII Ship Photography Website — full site
purpose: build-substrate for Angular SPA implementation
altitude: feature
updated: 2026-07-29T21:19
---

- (event) Architecture run started; fast-path; sources: prd.md, DESIGN.md, EXPERIENCE.md
- (version) Angular 22.1 (released 2026-07-27, current active); TypeScript ~5.8 [ASSUMPTION: Angular 22 compat range]; Node.js 22.x LTS (dev toolchain only)
- (decision) AD-1: Standalone Angular components (no NgModules) — Binds: all components; Prevents: NgModule-based bootstrap and module-scoped providers
- (decision) AD-2: HashLocationStrategy — Binds: Angular Router config; Prevents: PathLocationStrategy (breaks on FTP host without .htaccess rewrite)
- (decision) AD-3: Single typed ships.ts data file as sole source of truth — Binds: ShipDataService, routing, fleet order, homepage hero; Prevents: per-component data, split JSON files
- (decision) AD-4: CSS custom properties (_tokens.scss) for all DESIGN.md tokens — Binds: all component SCSS; Prevents: hard-coded hex values in component files
- (decision) AD-5: WitnessTrioBlock is an atomic Angular component — Binds: ShipPageComponent; Prevents: rendering DossierCard, AttributionCaption, or NarrativeSection independently outside the trio
- (decision) AD-6: Offline image pipeline (sharp script) — input: ./images/ JPEGs; outputs: src/assets/images/hero/ (1920w) and src/assets/images/thumb/ (600w), both WebP + JPEG fallback; Prevents: raw JPEG delivery, missing WebP fallback
- (decision) AD-7: ShipDataService is root-scope singleton Injectable — Binds: all route-level components; Prevents: multiple service instances, inconsistent fleet order
- (decision) AD-8: Deploy from dist/epic-ww2/browser/ — FTP contents of this directory to Bluehost public_html/ root; Prevents: nested path deployment, uploading wrong build output
- (decision) AD-9: TitleStrategy custom provider sets per-route <title> from ship name or route label — Binds: all routes; Prevents: static 'epic-ww2' title across all pages
- (decision) AD-10: Dependency direction — features→shared→models; features→core/services→data; NEVER shared→features; NEVER services→features
- (constraint) [ADOPTED] HashLocationStrategy — set in PRD FR-28
- (constraint) [ADOPTED] TypeScript data files for ship data — set in PRD FR-30
- (constraint) [ADOPTED] Mobile-first responsive layout — set in PRD FR-31
- (assumption) TypeScript version ~5.8 assumed compatible with Angular 22.1 — verify with 'ng new' output at project init time
- (assumption) No SSR or prerendering — all rendering is client-side; SEO not a stated requirement for this family history site
- (assumption) No state management library — ShipDataService provides static array; no reactive store needed
- (assumption) Grain texture implemented as body::after CSS pseudo-element with inline SVG noise — no external texture asset required
- (event) spine finalized; architecture-document.md produced
